# General
abbr -a v nvim
abbr -a oc opencode
abbr -a ll 'ls -lah'
abbr -a la 'ls -la'

# Git — basic set (plugin-git adds the full 100+ set on top of these)
abbr -a g git
abbr -a gs 'git status'
abbr -a gl 'git log --oneline -20'
abbr -a gd 'git diff'
abbr -a ga 'git add'
abbr -a gc 'git commit'
abbr -a gp 'git push'
